# Premier programme Baz!!!

fun foo(n: int): int {
  var x = 0;

  while n < 100 do {
    x = x + n;
    n = n + 1;
  }
  return x;
}


#fun times(param: int): anon(int)->int {
 #   return lambda(i: int): int { return i*param; };
#} 

#fun greet(name: string){	
#}

#fun add(): closure{
#	var p = 0;
#	return fun(){p = p + 1};
#}

fun bar(f: anon(int,int)->int, o: int) : int{
  return f(o,o);
}



{
  print "Hello world!";
  print ;

  var n = 3;
  

  #var timestwo = times(2);
  #print timestwo(5); 
  
  var f = lambda(x:int,y:int):int {return x*y;};
  
  print bar(f,n);
  print;
  
  print f(2,2);
  print;
  
  
  

  #print add(8);
  

  if n < 10 then {
    print "this is a small number!";
    print;
  }

  
  print foo(5);
  #print "The result is: " + foo(n);
  print;
}
