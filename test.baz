# Premier programme Baz!!!

fun foo(n: int): int {
  var x = 0;

  while n < 100 do {
    x = x + n;
    n = n + 1;
  }
  return x;
}


#fun times(param: int): anon {
#    return lambda(i: int): int { return i*param; };
#} 

#fun greet(name: string){	
#}

#fun add(): closure{
#	var p = 0;
#	return fun(){p = p + 1};
#}




{
  print "Hello world!";
  print ;

  var n = 3;
  

 # var timestwo = times(2);
  #timestwo(5); 
  
  var f = lambda(x:int):int {return x+1;};
  
  print f(0);
  print;
  

  #print add(8);
  

  if n < 10 then {
    print "this is a small number!";
    print;
  }

  
  #foo(5);
  #print "The result is: " + foo(n);
  print;
}
